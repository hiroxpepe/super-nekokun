using UnityEngine;

namespace StudioMeowToon {
    /// <summary>
    /// プロシージャル生成用
    /// </summary>
    public class ProceduralController : MonoBehaviour {
        // Awake is called when the script instance is being loaded.
        //void Awake() {
        //    foreach (Transform _child in transform) {
        //        Debug.Log(_child.gameObject.name);
        //        _child.position = new Vector3(
        //            _child.position.x + Random.Range(-0.5f, 0.5f),
        //            _child.position.y + Random.Range(-0.5f, 0.5f),
        //            _child.position.z + Random.Range(-0.5f, 0.5f)
        //        );
        //    }
        //}
    }

}
